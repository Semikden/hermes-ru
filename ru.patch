diff --git a/gateway/run.py b/gateway/run.py
index 8fda2c1f..a714a170 100644
--- a/gateway/run.py
+++ b/gateway/run.py
@@ -597,7 +597,7 @@ def _format_gateway_process_notification(evt: dict) -> "str | None":
     _cmd = evt.get("command", "unknown")
 
     if evt_type == "watch_disabled":
-        return f"[IMPORTANT: {evt.get('message', '')}]"
+        return f"[ВАЖНО: {evt.get('message', '')}]"
 
     if evt_type == "watch_match":
         _pat = evt.get("pattern", "?")
@@ -1621,13 +1621,13 @@ class GatewayRunner:
         status_detail = f" ({', '.join(status_parts)})" if status_parts else ""
         if is_queue_mode:
             message = (
-                f"⏳ Queued for the next turn{status_detail}. "
-                f"I'll respond once the current task finishes."
+                f"⏳ В очереди на следующий ход{status_detail}. "
+                f"Отвечу когда текущая задача завершится."
             )
         else:
             message = (
-                f"⚡ Interrupting current task{status_detail}. "
-                f"I'll respond to your message shortly."
+                f"⚡ Прерываю текущую задачу{status_detail}. "
+                f"Скоро отвечу на твоё сообщение."
             )
 
         # First-touch onboarding: the very first time a user sends a message
@@ -2712,6 +2712,24 @@ class GatewayRunner:
                 # catch-all cleanup below still runs for the graceful path.
                 _kill_tool_subprocesses("post-interrupt")
 
+            # For restart (not graceful shutdown): always mark active sessions
+            # for resume, even if drain completed without timeout.  The drain
+            # snapshot (active_agents) is authoritative here — it captures what
+            # was running when shutdown began.  self._running_agents is already
+            # empty when timed_out=False because agents completed gracefully
+            # before the drain window closed, so we use the snapshot instead.
+            elif self._restart_requested and active_agents:
+                _resume_reason = "restart_requested"
+                for _sk, _agent in active_agents.items():
+                    if _agent is _AGENT_PENDING_SENTINEL:
+                        continue
+                    try:
+                        self.session_store.mark_resume_pending(_sk, _resume_reason)
+                    except Exception as _e:
+                        logger.debug(
+                            "restart mark_resume_pending failed for %s: %s", _sk, _e,
+                        )
+
             if self._restart_requested and self._restart_detached:
                 try:
                     await self._launch_detached_restart_command()
@@ -3395,7 +3413,7 @@ class GatewayRunner:
                     invalidation_reason="stop_command",
                 )
                 logger.info("STOP for session %s — agent interrupted, session lock released", _quick_key)
-                return "⚡ Stopped. You can continue this session."
+                return "⚡ Остановлено. Можешь продолжить эту сессию."
 
             # /reset and /new must bypass the running-agent guard so they
             # actually dispatch as commands instead of being queued as user
@@ -3420,7 +3438,7 @@ class GatewayRunner:
             if event.get_command() in ("queue", "q"):
                 queued_text = event.get_command_args().strip()
                 if not queued_text:
-                    return "Usage: /queue <prompt>"
+                    return "Использование: /queue <текст>"
                 adapter = self.adapters.get(source.platform)
                 if adapter:
                     queued_event = MessageEvent(
@@ -3431,7 +3449,7 @@ class GatewayRunner:
                         channel_prompt=event.channel_prompt,
                     )
                     adapter._pending_messages[_quick_key] = queued_event
-                return "Queued for the next turn."
+                return "⏳ В очереди на следующий ход."
 
             # /steer <prompt> — inject mid-run after the next tool call.
             # Unlike /queue (turn boundary), /steer lands BETWEEN tool-call
@@ -3441,7 +3459,7 @@ class GatewayRunner:
             if _cmd_def_inner and _cmd_def_inner.name == "steer":
                 steer_text = event.get_command_args().strip()
                 if not steer_text:
-                    return "Usage: /steer <prompt>"
+                    return "Использование: /steer <текст>"
                 running_agent = self._running_agents.get(_quick_key)
                 if running_agent is _AGENT_PENDING_SENTINEL:
                     # Agent hasn't started yet — queue as turn-boundary fallback.
@@ -3455,7 +3473,7 @@ class GatewayRunner:
                             channel_prompt=event.channel_prompt,
                         )
                         adapter._pending_messages[_quick_key] = queued_event
-                    return "Agent still starting — /steer queued for the next turn."
+                    return "Агент все еще запускается — /steer в очереди на следующий ход."
                 if running_agent and hasattr(running_agent, "steer"):
                     try:
                         accepted = running_agent.steer(steer_text)
@@ -3465,7 +3483,7 @@ class GatewayRunner:
                     if accepted:
                         preview = steer_text[:60] + ("..." if len(steer_text) > 60 else "")
                         return f"⏩ Steer queued — arrives after the next tool call: '{preview}'"
-                    return "Steer rejected (empty payload)."
+                    return "Управление отклонено (пустая нагрузка)."
                 # Running agent is missing or lacks steer() — fall back to queue.
                 adapter = self.adapters.get(source.platform)
                 if adapter:
@@ -3477,11 +3495,11 @@ class GatewayRunner:
                         channel_prompt=event.channel_prompt,
                     )
                     adapter._pending_messages[_quick_key] = queued_event
-                return "No active agent — /steer queued for the next turn."
+                return "Нет активного агента — /steer в очереди на следующий ход."
 
             # /model must not be used while the agent is running.
             if _cmd_def_inner and _cmd_def_inner.name == "model":
-                return "Agent is running — wait or /stop first, then switch models."
+                return "Агент работает — подожди или сначала /stop, затем переключи модели."
 
             # /approve and /deny must bypass the running-agent interrupt path.
             # The agent thread is blocked on a threading.Event inside
@@ -3584,7 +3602,7 @@ class GatewayRunner:
                     # Force-clean the sentinel so the session is unlocked.
                     self._release_running_agent_state(_quick_key)
                     logger.info("HARD STOP (pending) for session %s — sentinel cleared", _quick_key)
-                    return "⚡ Force-stopped. The agent was still starting — session unlocked."
+                    return "⚡ Принудительно остановлено. Агент все еще запускался — сессия разблокирована."
                 # Queue the message so it will be picked up after the
                 # agent starts.
                 adapter = self.adapters.get(source.platform)
@@ -3668,7 +3686,7 @@ class GatewayRunner:
                     message = hook_result.get("message")
                     if isinstance(message, str) and message:
                         return message
-                    return f"Command `/{command}` was blocked by a hook."
+                    return f"Команда `/{command}` заблокирована хуком."
                 if decision == "handled":
                     message = hook_result.get("message")
                     return message if isinstance(message, str) and message else None
@@ -3781,7 +3799,7 @@ class GatewayRunner:
             # message. If the payload is empty, surface the usage hint.
             steer_payload = event.get_command_args().strip()
             if not steer_payload:
-                return "Usage: /steer <prompt>  (no agent is running; sending as a normal message)"
+                return "Использование: /steer <текст>  (агент не работает; отправка как обычное сообщение)"
             try:
                 event.text = steer_payload
             except Exception:
@@ -3819,11 +3837,11 @@ class GatewayRunner:
                             output = (stdout or stderr).decode().strip()
                             return output if output else "Command returned no output."
                         except asyncio.TimeoutError:
-                            return "Quick command timed out (30s)."
+                            return "Время выполнения быстрой команды истекло (30 секунд)."
                         except Exception as e:
-                            return f"Quick command error: {e}"
+                            return f"Ошибка быстрой команды: {e}"
                     else:
-                        return f"Quick command '/{command}' has no command defined."
+                        return f"Быстрая команда '/{command}': команда не определена."
                 elif qcmd.get("type") == "alias":
                     target = qcmd.get("target", "").strip()
                     if target:
@@ -3834,9 +3852,9 @@ class GatewayRunner:
                         command = target_command
                         # Fall through to normal command dispatch below
                     else:
-                        return f"Quick command '/{command}' has no target defined."
+                        return f"Быстрая команда '/{command}': цель не определена."
                 else:
-                    return f"Quick command '/{command}' has unsupported type (supported: 'exec', 'alias')."
+                    return f"Быстрая команда '/{command}': неподдерживаемый тип (допустимо: 'exec', 'alias')."
 
         # Plugin-registered slash commands
         if command:
@@ -4193,12 +4211,12 @@ class GatewayRunner:
                             hours = policy.idle_minutes // 60
                             mins = policy.idle_minutes % 60
                             duration = f"{hours}h" if not mins else f"{hours}h {mins}m" if hours else f"{mins}m"
-                            reason_text = f"inactive for {duration}"
+                            reason_text = f"неактивна {duration}"
                         notice = (
-                            f"◐ Session automatically reset ({reason_text}). "
-                            f"Conversation history cleared.\n"
-                            f"Use /resume to browse and restore a previous session.\n"
-                            f"Adjust reset timing in config.yaml under session_reset."
+                            f"◐ Сессия сброшена ({reason_text}). "
+                            f"История диалога очищена.\n"
+                            f"Используй /resume чтобы найти и восстановить предыдущую сессию.\n"
+                            f"Настроить тайминг сброса: config.yaml → session_reset."
                         )
                         try:
                             session_info = self._format_session_info()
@@ -4522,11 +4540,11 @@ class GatewayRunner:
                 if adapter:
                     await adapter.send(
                         source.chat_id,
-                        f"📬 No home channel is set for {platform_name.title()}. "
-                        f"A home channel is where Hermes delivers cron job results "
-                        f"and cross-platform messages.\n\n"
-                        f"Type /sethome to make this chat your home channel, "
-                        f"or ignore to skip."
+                        f"📬 Домашний канал для {platform_name.title()} не задан. "
+                        f"Сюда Hermes доставляет результаты cron-задач "
+                        f"и сообщения с других платформ.\n\n"
+                        f"Напиши /sethome чтобы назначить этот чат домашним, "
+                        f"или проигнорируй это сообщение."
                     )
         
         # -----------------------------------------------------------------
@@ -5305,7 +5323,7 @@ class GatewayRunner:
                 invalidation_reason="stop_command_pending",
             )
             logger.info("STOP (pending) for session %s — sentinel cleared", session_key)
-            return "⚡ Stopped. The agent hadn't started yet — you can continue this session."
+            return "⚡ Остановлено. Агент еще не запустился — ты можешь продолжить эту сессию."
         if agent:
             # Force-clean the session lock so a truly hung agent doesn't
             # keep it locked forever.
@@ -5315,9 +5333,9 @@ class GatewayRunner:
                 interrupt_reason=_INTERRUPT_REASON_STOP,
                 invalidation_reason="stop_command_handler",
             )
-            return "⚡ Stopped. You can continue this session."
+            return "⚡ Остановлено. Можешь продолжить эту сессию."
         else:
-            return "No active task to stop."
+            return "Нет активной задачи для остановки."
 
     async def _handle_restart_command(self, event: MessageEvent) -> str:
         """Handle /restart command - drain active work, then restart the gateway."""
@@ -5344,8 +5362,8 @@ class GatewayRunner:
         if self._restart_requested or self._draining:
             count = self._running_agent_count()
             if count:
-                return f"⏳ Draining {count} active agent(s) before restart..."
-            return "⏳ Gateway restart already in progress..."
+                return f"⏳ Завершаю {count} активных агент(ов) перед рестартом..."
+            return "⏳ Рестарт шлюза уже идёт..."
 
         # Save the requester's routing info so the new gateway process can
         # notify them once it comes back online.
@@ -5393,7 +5411,7 @@ class GatewayRunner:
             self.request_restart(detached=True, via_service=False)
         if active_agents:
             return f"⏳ Draining {active_agents} active agent(s) before restart..."
-        return "♻ Restarting gateway. If you aren't notified within 60 seconds, restart from the console with `hermes gateway restart`."
+        return "♻️ Перезапускаю шлюз. Если не отвечу в течение 60 секунд — перезапусти вручную: hermes gateway restart"
 
     def _is_stale_restart_redelivery(self, event: MessageEvent) -> bool:
         """Return True if this /restart is a Telegram re-delivery we already handled.
@@ -5476,7 +5494,7 @@ class GatewayRunner:
             try:
                 requested_page = int(raw_args)
             except ValueError:
-                return "Usage: `/commands [page]`"
+                return "Использование: `/commands [страница]`"
         else:
             requested_page = 1
 
@@ -5487,7 +5505,7 @@ class GatewayRunner:
             skill_cmds = get_skill_commands()
             if skill_cmds:
                 entries.append("")
-                entries.append("⚡ **Skill Commands**:")
+                entries.append("⚡ **Команды скиллов**:")
                 for cmd in sorted(skill_cmds):
                     desc = skill_cmds[cmd].get("description", "").strip() or "Skill command"
                     entries.append(f"`{cmd}` — {desc}")
@@ -5495,7 +5513,7 @@ class GatewayRunner:
             pass
 
         if not entries:
-            return "No commands available."
+            return "Нет доступных команд."
 
         from gateway.config import Platform
         page_size = 15 if event.source.platform == Platform.TELEGRAM else 20
@@ -5505,19 +5523,19 @@ class GatewayRunner:
         page_entries = entries[start:start + page_size]
 
         lines = [
-            f"📚 **Commands** ({len(entries)} total, page {page}/{total_pages})",
+            f"📚 **Команды** ({len(entries)}, страница {page}/{total_pages})",
             "",
             *page_entries,
         ]
         if total_pages > 1:
             nav_parts = []
             if page > 1:
-                nav_parts.append(f"`/commands {page - 1}` ← prev")
+                nav_parts.append(f"`← prev` / `/commands {page - 1}`")
             if page < total_pages:
-                nav_parts.append(f"next → `/commands {page + 1}`")
+                nav_parts.append(f"`/commands {page + 1}` → next")
             lines.extend(["", " | ".join(nav_parts)])
         if page != requested_page:
-            lines.append(f"_(Requested page {requested_page} was out of range, showing page {page}.)_")
+            lines.append(f"_(Запрошенная страница {requested_page} вне диапазона, показана страница {page}.)_")
         return "\n".join(lines)
     
     async def _handle_model_command(self, event: MessageEvent) -> Optional[str]:
@@ -5625,7 +5643,7 @@ class GatewayRunner:
                             custom_providers=custom_provs,
                         )
                         if not result.success:
-                            return f"Error: {result.error_message}"
+                            return f"Ошибка: {result.error_message}"
 
                         # Update cached agent in-place
                         cached_entry = None
@@ -5749,7 +5767,7 @@ class GatewayRunner:
         )
 
         if not result.success:
-            return f"Error: {result.error_message}"
+            return f"Ошибка: {result.error_message}"
 
         # If there's a cached agent, update it in-place
         cached_entry = None
@@ -5910,7 +5928,7 @@ class GatewayRunner:
             except Exception as e:
                 return f"⚠️ Failed to save personality change: {e}"
             self._ephemeral_system_prompt = ""
-            return "🎭 Personality cleared — using base agent behavior.\n_(takes effect on next message)_"
+            return "🎭 Личность очищена — используется базовое поведение агента.\n_(вступает в силу с следующего сообщения)_"
         elif args in personalities:
             new_prompt = _resolve_prompt(personalities[args])
 
@@ -5926,7 +5944,7 @@ class GatewayRunner:
             # Update in-memory so it takes effect on the very next message.
             self._ephemeral_system_prompt = new_prompt
 
-            return f"🎭 Personality set to **{args}**\n_(takes effect on next message)_"
+            return f"🎭 Личность установлена на **{args}**\n_(вступает в силу со следующего сообщения)_"
 
         available = "`none`, " + ", ".join(f"`{n}`" for n in personalities)
         return f"Unknown personality: `{args}`\n\nAvailable: {available}"
@@ -5947,7 +5965,7 @@ class GatewayRunner:
                 break
         
         if not last_user_msg:
-            return "No previous message to retry."
+            return "Нет предыдущего сообщения для повторной попытки."
         
         # Truncate history to before the last user message and persist
         truncated = history[:last_user_idx]
@@ -5981,7 +5999,7 @@ class GatewayRunner:
                 break
         
         if last_user_idx is None:
-            return "Nothing to undo."
+            return "Нечего отменять."
         
         removed_msg = history[last_user_idx].get("content", "")
         removed_count = len(history) - last_user_idx
@@ -5990,7 +6008,7 @@ class GatewayRunner:
         session_entry.last_prompt_tokens = 0
         
         preview = removed_msg[:40] + "..." if len(removed_msg) > 40 else removed_msg
-        return f"↩️ Undid {removed_count} message(s).\nRemoved: \"{preview}\""
+        return f"↩️ Отменено {removed_count} сообщение(й).\nУдалено: \"{preview}\""
     
     async def _handle_set_home_command(self, event: MessageEvent) -> str:
         """Handle /sethome command -- set the current chat as the platform's home channel."""
@@ -6059,7 +6077,7 @@ class GatewayRunner:
             self._save_voice_modes()
             if adapter:
                 self._set_adapter_auto_tts_disabled(adapter, chat_id, disabled=True)
-            return "Voice mode disabled. Text-only replies."
+            return "Голосовой режим отключен. Ответы только текстом."
         elif args == "tts":
             self._voice_mode[voice_key] = "all"
             self._save_voice_modes()
@@ -6104,29 +6122,29 @@ class GatewayRunner:
                 self._save_voice_modes()
                 if adapter:
                     self._set_adapter_auto_tts_enabled(adapter, chat_id, enabled=True)
-                return "Voice mode enabled."
+                return "Голосовой режим включен."
             else:
                 self._voice_mode[voice_key] = "off"
                 self._save_voice_modes()
                 if adapter:
                     self._set_adapter_auto_tts_disabled(adapter, chat_id, disabled=True)
-                return "Voice mode disabled."
+                return "Голосовой режим отключен."
 
     async def _handle_voice_channel_join(self, event: MessageEvent) -> str:
         """Join the user's current Discord voice channel."""
         adapter = self.adapters.get(event.source.platform)
         if not hasattr(adapter, "join_voice_channel"):
-            return "Voice channels are not supported on this platform."
+            return "Голосовые каналы не поддерживаются на этой платформе."
 
         guild_id = self._get_guild_id(event)
         if not guild_id:
-            return "This command only works in a Discord server."
+            return "Эта команда работает только на сервере Discord."
 
         voice_channel = await adapter.get_user_voice_channel(
             guild_id, event.source.user_id
         )
         if not voice_channel:
-            return "You need to be in a voice channel first."
+            return "Сначала нужно быть в голосовом канале."
 
         # Wire callbacks BEFORE join so voice input arriving immediately
         # after connection is not lost.
@@ -6161,7 +6179,7 @@ class GatewayRunner:
             )
         # Join failed — clear callback
         adapter._voice_input_callback = None
-        return "Failed to join voice channel. Check bot permissions (Connect + Speak)."
+        return "Не удалось присоединиться к голосовому каналу. Проверьте разрешения бота (Подключение + Говорить)."
 
     async def _handle_voice_channel_leave(self, event: MessageEvent) -> str:
         """Leave the Discord voice channel."""
@@ -6169,10 +6187,10 @@ class GatewayRunner:
         guild_id = self._get_guild_id(event)
 
         if not guild_id or not hasattr(adapter, "leave_voice_channel"):
-            return "Not in a voice channel."
+            return "Не в голосовом канале."
 
         if not hasattr(adapter, "is_in_voice_channel") or not adapter.is_in_voice_channel(guild_id):
-            return "Not in a voice channel."
+            return "Не в голосовом канале."
 
         try:
             await adapter.leave_voice_channel(guild_id)
@@ -6184,7 +6202,7 @@ class GatewayRunner:
         self._set_adapter_auto_tts_disabled(adapter, event.source.chat_id, disabled=True)
         if hasattr(adapter, "_voice_input_callback"):
             adapter._voice_input_callback = None
-        return "Left voice channel."
+        return "Покинул голосовой канал."
 
     def _handle_voice_timeout_cleanup(self, chat_id: str) -> None:
         """Called by the adapter when a voice channel times out.
@@ -6748,17 +6766,17 @@ class GatewayRunner:
         if args in ("hide", "off"):
             self._show_reasoning = False
             _save_config_key(f"display.platforms.{platform_key}.show_reasoning", False)
-            return f"🧠 ✓ Reasoning display: **OFF** for **{platform_key}**"
+            return f"🧠 ✓ Отображение рассуждений: **ВЫКЛ** для **{platform_key}**"
 
         # Effort level change
         effort = args.strip()
         if effort == "reset":
             if persist_global:
-                return "⚠️ `/reasoning reset --global` is not supported. Use `/reasoning <level> --global` to change the global default."
+                return "⚠️ `/reasoning reset --global` не поддерживается. Используйте `/reasoning <уровень> --global` вместо этого."
             self._set_session_reasoning_override(session_key, None)
             self._reasoning_config = self._load_reasoning_config()
             self._evict_cached_agent(session_key)
-            return "🧠 ✓ Session reasoning override cleared; falling back to global config."
+            return "🧠 ✓ Переопределение рассуждений сессии очищено; возвращение к глобальной конфигурации."
         if effort == "none":
             parsed = {"enabled": False}
         elif effort in ("minimal", "low", "medium", "high", "xhigh"):
@@ -6776,14 +6794,14 @@ class GatewayRunner:
             if _save_config_key("agent.reasoning_effort", effort):
                 self._set_session_reasoning_override(session_key, None)
                 self._evict_cached_agent(session_key)
-                return f"🧠 ✓ Reasoning effort set to `{effort}` (saved to config)\n_(takes effect on next message)_"
+                return f"🧠 ✓ Усилие рассуждений установлено на `{effort}` (сохранено в конфигурации)\n_(вступает в силу со следующего сообщения)_"
             self._set_session_reasoning_override(session_key, parsed)
             self._evict_cached_agent(session_key)
-            return f"🧠 ✓ Reasoning effort set to `{effort}` (session only — config save failed)\n_(takes effect on next message)_"
+            return f"🧠 ✓ Усилие рассуждений установлено на `{effort}` (только сессия — сохранение конфига не удалось)\n_(вступает в силу со следующего сообщения)_"
 
         self._set_session_reasoning_override(session_key, parsed)
         self._evict_cached_agent(session_key)
-        return f"🧠 ✓ Reasoning effort set to `{effort}` (session only — add `--global` to persist)\n_(takes effect on next message)_"
+        return f"🧠 ✓ Усилие рассуждений установлено на `{effort}` (только сессия — добавь `--global` для сохранения)\n_(вступает в силу со следующего сообщения)_"
 
     async def _handle_fast_command(self, event: MessageEvent) -> str:
         """Handle /fast — mirror the CLI Priority Processing toggle in gateway chats."""
@@ -6797,7 +6815,7 @@ class GatewayRunner:
         user_config = _load_gateway_config()
         model = _resolve_gateway_model(user_config)
         if not model_supports_fast_mode(model):
-            return "⚡ /fast is only available for OpenAI models that support Priority Processing."
+            return "⚡ /fast доступна только для моделей OpenAI, которые поддерживают приоритетную обработку."
 
         def _save_config_key(key_path: str, value):
             """Save a dot-separated key to config.yaml."""
@@ -6842,8 +6860,8 @@ class GatewayRunner:
             )
 
         if _save_config_key("agent.service_tier", saved_value):
-            return f"⚡ ✓ Priority Processing: **{label}** (saved to config)\n_(takes effect on next message)_"
-        return f"⚡ ✓ Priority Processing: **{label}** (this session only)"
+            return f"⚡ ✓ Приоритетная обработка: **{label}** (сохранено в конфигурации)\n_(вступает в силу со следующего сообщения)_"
+        return f"⚡ ✓ Приоритетная обработка: **{label}** (только эта сессия)"
 
     async def _handle_yolo_command(self, event: MessageEvent) -> str:
         """Handle /yolo — toggle dangerous command approval bypass for this session only."""
@@ -6857,10 +6875,10 @@ class GatewayRunner:
         current = is_session_yolo_enabled(session_key)
         if current:
             disable_session_yolo(session_key)
-            return "⚠️ YOLO mode **OFF** for this session — dangerous commands will require approval."
+            return "⚠️ Режим YOLO **ВЫКЛ** для этой сессии — опасные команды потребуют одобрения."
         else:
             enable_session_yolo(session_key)
-            return "⚡ YOLO mode **ON** for this session — all commands auto-approved. Use with caution."
+            return "⚡ Режим YOLO **ВКЛ** для этой сессии — все команды автоматически одобрены. Используйте с осторожностью."
 
     async def _handle_verbose_command(self, event: MessageEvent) -> str:
         """Handle /verbose command — cycle tool progress display mode.
@@ -6941,7 +6959,7 @@ class GatewayRunner:
         history = self.session_store.load_transcript(session_entry.session_id)
 
         if not history or len(history) < 4:
-            return "Not enough conversation to compress (need at least 4 messages)."
+            return "Недостаточно разговора для сжатия (нужно как минимум 4 сообщения)."
 
         # Extract optional focus topic from command args
         focus_topic = (event.get_command_args() or "").strip() or None
@@ -6957,7 +6975,7 @@ class GatewayRunner:
                 session_key=session_key,
             )
             if not runtime_kwargs.get("api_key"):
-                return "No provider configured -- cannot compress."
+                return "Поставщик не настроен — не могу сжать."
 
             msgs = [
                 {"role": m.get("role"), "content": m.get("content")}
@@ -6981,7 +6999,7 @@ class GatewayRunner:
 
                 compressor = tmp_agent.context_compressor
                 if not compressor.has_content_to_compress(msgs):
-                    return "Nothing to compress yet (the transcript is still all protected context)."
+                    return "Нечего сжимать пока (транскрипция все еще защищена)."
 
                 loop = asyncio.get_running_loop()
                 compressed, _ = await loop.run_in_executor(
@@ -7030,7 +7048,7 @@ class GatewayRunner:
         session_id = session_entry.session_id
 
         if not self._session_db:
-            return "Session database not available."
+            return "База данных сессий недоступна."
 
         # Ensure session exists in SQLite DB (it may only exist in session_store
         # if this is the first command in a new session)
@@ -7054,27 +7072,27 @@ class GatewayRunner:
             except ValueError as e:
                 return f"⚠️ {e}"
             if not sanitized:
-                return "⚠️ Title is empty after cleanup. Please use printable characters."
+                return "⚠️ Заголовок пуст после очистки. Пожалуйста, используйте печатные символы."
             # Set the title
             try:
                 if self._session_db.set_session_title(session_id, sanitized):
-                    return f"✏️ Session title set: **{sanitized}**"
+                    return f"✏️ Заголовок сессии установлен: **{sanitized}**"
                 else:
-                    return "Session not found in database."
+                    return "Сессия не найдена в базе данных."
             except ValueError as e:
                 return f"⚠️ {e}"
         else:
             # Show the current title and session ID
             title = self._session_db.get_session_title(session_id)
             if title:
-                return f"📌 Session: `{session_id}`\nTitle: **{title}**"
+                return f"📌 Сессия: `{session_id}`\nЗаголовок: **{title}**"
             else:
-                return f"📌 Session: `{session_id}`\nNo title set. Usage: `/title My Session Name`"
+                return f"📌 Сессия: `{session_id}`\nЗаголовок не установлен. Использование: `/title Название Сессии`"
 
     async def _handle_resume_command(self, event: MessageEvent) -> str:
         """Handle /resume command — switch to a previously-named session."""
         if not self._session_db:
-            return "Session database not available."
+            return "База данных сессий недоступна."
 
         source = event.source
         session_key = self._session_key_for_source(source)
@@ -7104,7 +7122,7 @@ class GatewayRunner:
                 return "\n".join(lines)
             except Exception as e:
                 logger.debug("Failed to list titled sessions: %s", e)
-                return f"Could not list sessions: {e}"
+                return f"Не удалось получить список сессий: {e}"
 
         # Resolve the name to a session ID.
         target_id = self._session_db.resolve_session_by_title(name)
@@ -7123,7 +7141,7 @@ class GatewayRunner:
         # Check if already on that session
         current_entry = self.session_store.get_or_create_session(source)
         if current_entry.session_id == target_id:
-            return f"📌 Already on session **{name}**."
+            return f"📌 Уже в сессии **{name}**."
 
         # Clear any running agent for this session key
         self._release_running_agent_state(session_key)
@@ -7131,7 +7149,7 @@ class GatewayRunner:
         # Switch the session entry to point at the old session
         new_entry = self.session_store.switch_session(session_key, target_id)
         if not new_entry:
-            return "Failed to switch session."
+            return "Не удалось переключить сессию."
         self._clear_session_boundary_security_state(session_key)
 
         # Get the title for confirmation
@@ -7142,7 +7160,7 @@ class GatewayRunner:
         msg_count = len([m for m in history if m.get("role") == "user"]) if history else 0
         msg_part = f" ({msg_count} message{'s' if msg_count != 1 else ''})" if msg_count else ""
 
-        return f"↻ Resumed session **{title}**{msg_part}. Conversation restored."
+        return f"↻ Сессия **{title}** восстановлена{msg_part}. Диалог возобновлён."
 
     async def _handle_branch_command(self, event: MessageEvent) -> str:
         """Handle /branch [name] — fork the current session into a new independent copy.
@@ -7154,7 +7172,7 @@ class GatewayRunner:
         import uuid as _uuid
 
         if not self._session_db:
-            return "Session database not available."
+            return "База данных сессий недоступна."
 
         source = event.source
         session_key = self._session_key_for_source(source)
@@ -7163,7 +7181,7 @@ class GatewayRunner:
         current_entry = self.session_store.get_or_create_session(source)
         history = self.session_store.load_transcript(current_entry.session_id)
         if not history:
-            return "No conversation to branch — send a message first."
+            return "Нет разговора для ветвления — сначала отправьте сообщение."
 
         branch_name = event.get_command_args().strip()
 
@@ -7221,7 +7239,7 @@ class GatewayRunner:
         # Switch the session store entry to the new session
         new_entry = self.session_store.switch_session(session_key, new_session_id)
         if not new_entry:
-            return "Branch created but failed to switch to it."
+            return "Ветка создана, но не удалось переключиться на нее."
         self._clear_session_boundary_security_state(session_key)
 
         # Evict any cached agent for this session
@@ -7371,7 +7389,7 @@ class GatewayRunner:
             return "\n".join(lines)
         if account_lines:
             return "\n".join(account_lines)
-        return "No usage data available for this session."
+        return "Нет данных о использовании для этой сессии."
 
     async def _handle_insights_command(self, event: MessageEvent) -> str:
         """Handle /insights command -- show usage insights and analytics."""
@@ -7420,7 +7438,7 @@ class GatewayRunner:
             return await loop.run_in_executor(None, _run_insights)
         except Exception as e:
             logger.error("Insights command error: %s", e, exc_info=True)
-            return f"Error generating insights: {e}"
+            return f"Ошибка генерации инсайтов: {e}"
 
     async def _handle_reload_mcp_command(self, event: MessageEvent) -> str:
         """Handle /reload-mcp command -- disconnect and reconnect all MCP servers."""
@@ -7525,8 +7543,8 @@ class GatewayRunner:
         if not has_blocking_approval(session_key):
             if session_key in self._pending_approvals:
                 self._pending_approvals.pop(session_key)
-                return "⚠️ Approval expired (agent is no longer waiting). Ask the agent to try again."
-            return "No pending command to approve."
+                return "⚠️ Одобрение истекло (агент больше не ждет). Попросите агента попробовать снова."
+            return "Нет ожидающей команды для одобрения."
 
         # Parse args: support "all", "all session", "all always", "session", "always"
         args = event.get_command_args().strip().lower().split()
@@ -7545,7 +7563,7 @@ class GatewayRunner:
 
         count = resolve_gateway_approval(session_key, choice, resolve_all=resolve_all)
         if not count:
-            return "No pending command to approve."
+            return "Нет ожидающей команды для одобрения."
 
         # Resume typing indicator — agent is about to continue processing.
         _adapter = self.adapters.get(source.platform)
@@ -7554,7 +7572,7 @@ class GatewayRunner:
 
         count_msg = f" ({count} commands)" if count > 1 else ""
         logger.info("User approved %d dangerous command(s) via /approve%s", count, scope_msg)
-        return f"✅ Command{'s' if count > 1 else ''} approved{scope_msg}{count_msg}. The agent is resuming..."
+        return f"✅ Команда{'ы' if count > 1 else ''} одобрена{scope_msg}{count_msg}. Агент продолжает..."
 
     async def _handle_deny_command(self, event: MessageEvent) -> str:
         """Handle /deny command — reject pending dangerous command(s).
@@ -7574,15 +7592,15 @@ class GatewayRunner:
         if not has_blocking_approval(session_key):
             if session_key in self._pending_approvals:
                 self._pending_approvals.pop(session_key)
-                return "❌ Command denied (approval was stale)."
-            return "No pending command to deny."
+                return "❌ Команда отклонена (одобрение устарело)."
+            return "Нет ожидающей команды для отклонения."
 
         args = event.get_command_args().strip().lower()
         resolve_all = "all" in args
 
         count = resolve_gateway_approval(session_key, "deny", resolve_all=resolve_all)
         if not count:
-            return "No pending command to deny."
+            return "Нет ожидающей команды для отклонения."
 
         # Resume typing indicator — agent continues (with BLOCKED result).
         _adapter = self.adapters.get(source.platform)
@@ -7591,7 +7609,7 @@ class GatewayRunner:
 
         count_msg = f" ({count} commands)" if count > 1 else ""
         logger.info("User denied %d dangerous command(s) via /deny", count)
-        return f"❌ Command{'s' if count > 1 else ''} denied{count_msg}."
+        return f"❌ Команда{'ы' if count > 1 else ''} отклонены{count_msg}."
 
     # Platforms where /update is allowed.  ACP, API server, and webhooks are
     # programmatic interfaces that should not trigger system updates.
@@ -7663,16 +7681,16 @@ class GatewayRunner:
         # Block non-messaging platforms (API server, webhooks, ACP)
         platform = event.source.platform
         if platform not in self._UPDATE_ALLOWED_PLATFORMS:
-            return "✗ /update is only available from messaging platforms. Run `hermes update` from the terminal."
+            return "✗ /update доступна только из мессенджеров. Запустите `hermes update` из терминала."
 
         if is_managed():
-            return f"✗ {format_managed_message('update Hermes Agent')}"
+            return f"✗ {format_managed_message('обновление Hermes')}"
 
         project_root = Path(__file__).parent.parent.resolve()
         git_dir = project_root / '.git'
 
         if not git_dir.exists():
-            return "✗ Not a git repository — cannot update."
+            return "✗ Не репозиторий git — не могу обновить."
 
         hermes_cmd = _resolve_hermes_bin()
         if not hermes_cmd:
@@ -7737,7 +7755,7 @@ class GatewayRunner:
             return f"✗ Failed to start update: {e}"
 
         self._schedule_update_notification_watch()
-        return "⚕ Starting Hermes update… I'll stream progress here."
+        return "⚕ Запуск обновления Hermes… Я буду транслировать прогресс здесь."
 
     def _schedule_update_notification_watch(self) -> None:
         """Ensure a background task is watching for update completion."""
@@ -7856,7 +7874,7 @@ class GatewayRunner:
                     exit_code_raw = exit_code_path.read_text().strip() or "1"
                     exit_code = int(exit_code_raw)
                     if exit_code == 0:
-                        await adapter.send(chat_id, "✅ Hermes update finished.")
+                        await adapter.send(chat_id, "✅ Обновление Hermes завершено.")
                     else:
                         await adapter.send(chat_id, "❌ Hermes update failed (exit code {}).".format(exit_code))
                     logger.info("Update finished (exit=%s), notified %s", exit_code, session_key)
@@ -7939,7 +7957,7 @@ class GatewayRunner:
             exit_code_path.write_text("124")
             await _flush_buffer()
             try:
-                await adapter.send(chat_id, "❌ Hermes update timed out after 30 minutes.")
+                await adapter.send(chat_id, "❌ Обновление Hermes истекло через 30 минут.")
             except Exception:
                 pass
             for p in (pending_path, claimed_path, output_path,
@@ -8061,7 +8079,7 @@ class GatewayRunner:
             metadata = {"thread_id": thread_id} if thread_id else None
             await adapter.send(
                 chat_id,
-                "♻ Gateway restarted successfully. Your session continues.",
+                "♻️ Шлюз успешно перезапущен. Сессия продолжается.",
                 metadata=metadata,
             )
             logger.info(
@@ -9289,6 +9307,43 @@ class GatewayRunner:
             if not progress_queue or not _run_still_current():
                 return
 
+            # Russian labels for Telegram progress messages (emoji comes from get_tool_emoji separately)
+            _RU = {
+                "terminal": "терминал",
+                "browser": "браузер",
+                "read_file": "читаю",
+                "write_file": "пишу",
+                "patch": "патчу",
+                "search_files": "ищу",
+                "vision_analyze": "анализирую",
+                "text_to_speech": "голос",
+                "execute_code": "код",
+                "delegate_task": "делегирую",
+                "session_search": "поиск сессий",
+                "skill_view": "скилл",
+                "skill_manage": "управление скиллом",
+                "cronjob": "крон",
+                "send_message": "отправляю",
+                "memory": "память",
+                "read_memory": "читаю память",
+                "patch_memory": "обновляю память",
+                "browser_navigate": "открываю",
+                "browser_click": "кликаю",
+                "browser_type": "ввожу",
+                "browser_scroll": "прокручиваю",
+                "browser_snapshot": "снимок",
+                "browser_vision": "снимок страницы",
+                "browser_console": "консоль",
+                "browser_get_images": "изображения",
+                "browser_back": "назад",
+                "browser_press": "нажимаю",
+                "process": "процесс",
+                "todo": "задачи",
+                "clarify": "уточняю",
+                "patch": "патчу",
+            }
+            tool_label = _RU.get(tool_name, tool_name)
+
             # First-touch onboarding: the first time a tool takes longer than
             # _LONG_TOOL_THRESHOLD_S during a run that's streaming every tool
             # (progress_mode == "all"), append a one-time hint suggesting
@@ -9355,11 +9410,11 @@ class GatewayRunner:
                     # detail.  Platform message-length limits handle the rest.
                     if _pl > 0 and len(args_str) > _pl:
                         args_str = args_str[:_pl - 3] + "..."
-                    msg = f"{emoji} {tool_name}({list(args.keys())})\n{args_str}"
+                    msg = f"{emoji} {tool_label}({list(args.keys())})\n{args_str}"
                 elif preview:
-                    msg = f"{emoji} {tool_name}: \"{preview}\""
+                    msg = f"{emoji} {tool_label}: \"{preview}\""
                 else:
-                    msg = f"{emoji} {tool_name}..."
+                    msg = f"{emoji} {tool_label}..."
                 progress_queue.put(msg)
                 return
             
@@ -9372,9 +9427,9 @@ class GatewayRunner:
                 _cap = _pl if _pl > 0 else 40
                 if len(preview) > _cap:
                     preview = preview[:_cap - 3] + "..."
-                msg = f"{emoji} {tool_name}: \"{preview}\""
+                msg = f"{emoji} {tool_label}: \"{preview}\""
             else:
-                msg = f"{emoji} {tool_name}..."
+                msg = f"{emoji} {tool_label}..."
             
             # Dedup: collapse consecutive identical progress messages.
             # Common with execute_code where models iterate with the same
diff --git a/hermes_cli/commands.py b/hermes_cli/commands.py
index 614d783d..0d6bd8b7 100644
--- a/hermes_cli/commands.py
+++ b/hermes_cli/commands.py
@@ -374,6 +374,77 @@ def _is_gateway_available(cmd: CommandDef, config_overrides: set[str] | None = N
     return False
 
 
+# Russian translations for command descriptions (used by gateway_help_lines)
+_CMD_DESC_RU = {
+    "Start a new session (fresh session ID + history)": "Начать новую сессию (новый ID + история)",
+    "Clear screen and start a new session": "Очистить экран и начать новую сессию",
+    "Show conversation history": "Показать историю разговора",
+    "Save the current conversation": "Сохранить текущий разговор",
+    "Retry the last message (resend to agent)": "Повторить последнее сообщение (отправить агенту заново)",
+    "Remove the last user/assistant exchange": "Удалить последний обмен пользователь/ассистент",
+    "Set a title for the current session": "Установить заголовок текущей сессии",
+    "Branch the current session (explore a different path)": "Создать ветку сессии (исследовать другой путь)",
+    "Manually compress conversation context": "Вручную сжать контекст разговора",
+    "List or restore filesystem checkpoints": "Список или восстановление контрольных точек",
+    "Create or restore state snapshots of Hermes config/state": "Создать или восстановить снапшоты состояния Hermes",
+    "Kill all running background processes": "Остановить все фоновые процессы",
+    "Approve a pending dangerous command": "Одобрить ожидающую опасную команду",
+    "Deny a pending dangerous command": "Отклонить ожидающую опасную команду",
+    "Run a prompt in the background": "Выполнить промпт в фоне",
+    "Show active agents and running tasks": "Показать активных агентов и задачи",
+    "Queue a prompt for the next turn (doesn't interrupt)": "Поставить промпт в очередь на следующий ход (не прерывает)",
+    "Inject a message after the next tool call without interrupting": "Вставить сообщение после следующего вызова инструмента без прерывания",
+    "Show session info": "Показать информацию о сессии",
+    "Show active profile name and home directory": "Показать имя активного профиля и домашнюю директорию",
+    "Set this chat as the home channel": "Назначить этот чат домашним каналом",
+    "Resume a previously-named session": "Возобновить именованную сессию",
+    "Show current configuration": "Показать текущую конфигурацию",
+    "Switch model for this session": "Переключить модель для этой сессии",
+    "Show Google Gemini Code Assist quota usage": "Показать использование квоты Google Gemini Code Assist",
+    "Set a predefined personality": "Установить предопределённую личность",
+    "Toggle the context/model status bar": "Переключить строку статуса контекста/модели",
+    "Cycle tool progress display: off -> new -> all -> verbose": "Цикл отображения прогресса: выкл -> новое -> всё -> подробно",
+    "Toggle YOLO mode (skip all dangerous command approvals)": "Переключить YOLO-режим (пропустить все подтверждения опасных команд)",
+    "Manage reasoning effort and display": "Управление усилием и отображением рассуждений",
+    "Toggle fast mode — OpenAI Priority Processing / Anthropic Fast Mode (Normal/Fast)": "Переключить быстрый режим — OpenAI Priority Processing / Anthropic Fast Mode",
+    "Show or change the display skin/theme": "Показать или изменить тему оформления",
+    "Toggle voice mode": "Переключить голосовой режим",
+    "Control what Enter does while Hermes is working": "Управление поведением Enter во время работы Hermes",
+    "Manage tools: /tools [list|disable|enable] [name...]": "Управление инструментами: /tools [список|выкл|вкл] [имена...]",
+    "List available toolsets": "Список доступных наборов инструментов",
+    "Search, install, inspect, or manage skills": "Искать, устанавливать, просматривать скиллы",
+    "Manage scheduled tasks": "Управление запланированными задачами",
+    "Reload .env variables into the running session": "Перезагрузить переменные .env в текущую сессию",
+    "Reload MCP servers from config": "Перезагрузить MCP-серверы из конфига",
+    "Connect browser tools to your live Chrome via CDP": "Подключить браузерные инструменты к живому Chrome через CDP",
+    "List installed plugins and their status": "Список установленных плагинов и их статус",
+    "Browse all commands and skills (paginated)": "Список всех команд и скиллов (с страницами)",
+    "Show available commands": "Показать доступные команды",
+    "Gracefully restart the gateway after draining active runs": "Плавный рестарт шлюза после завершения активных запусков",
+    "Show token usage and rate limits for the current session": "Показать использование токенов и лимиты для текущей сессии",
+    "Show usage insights and analytics": "Показать инсайты использования и аналитику",
+    "Show gateway/messaging platform status": "Показать статус шлюза и платформ",
+    "Copy the last assistant response to clipboard": "Копировать последний ответ ассистента в буфер",
+    "Attach clipboard image from your clipboard": "Прикрепить изображение из буфера обмена",
+    "Attach a local image file for your next prompt": "Прикрепить локальный файл изображения для следующего промпта",
+    "Update Hermes Agent to the latest version": "Обновить Hermes Agent до последней версии",
+    "Upload debug report (system info + logs) and get shareable links": "Загрузить отчёт об отладке (система + логи) и получить ссылки",
+    "Exit the CLI": "Выйти из CLI",
+    "Browse and manage sessions": "Просмотр и управление сессиями",
+    "Show Hermes version info": "Показать информацию о версии Hermes",
+    "Toggle token usage tracking": "Переключить отслеживание использования токенов",
+    "Start interactive debugger (pdb)": "Запустить интерактивный отладчик (pdb)",
+    "Show current system prompt": "Показать текущий системный промпт",
+    "Reload all skill registries": "Перезагрузить все реестры скиллов",
+    "Toggle verbose tool output": "Переключить подробный вывод инструментов",
+    "Search and replace in session history": "Искать и заменять в истории сессии",
+    "Load a named System Prompt template": "Загрузить шаблон системного промпта",
+    "Manage Hermes plugins": "Управление плагинами Hermes",
+    "Run a quick command (alias expansions + env var interpolation)": "Выполнить быструю команду (раскрытие алиасов + интерполяция env)",
+    "Toggle auto-selection of default model": "Переключить автовыбор модели по умолчанию",
+}
+
+
 def gateway_help_lines() -> list[str]:
     """Generate gateway help text lines from the registry."""
     overrides = _resolve_config_gates()
@@ -389,7 +460,8 @@ def gateway_help_lines() -> list[str]:
                 continue
             alias_parts.append(f"`/{a}`")
         alias_note = f" (alias: {', '.join(alias_parts)})" if alias_parts else ""
-        lines.append(f"`/{cmd.name}{args}` -- {cmd.description}{alias_note}")
+        desc_ru = _CMD_DESC_RU.get(cmd.description, cmd.description)
+        lines.append(f"`/{cmd.name}{args}` -- {desc_ru}{alias_note}")
     return lines
 
 
